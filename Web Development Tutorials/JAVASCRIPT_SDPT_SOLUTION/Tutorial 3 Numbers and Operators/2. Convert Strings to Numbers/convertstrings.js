// Uncessfull conversion will result into NaN (Not A Number) Value.

// Converts String to Integer.  
let num1 = "12"; 
let num2 = 2;  
alert("Not converted result: " +  num1 + num2); 

// If you need concatenation of  alert (sting + (parseInt (variable_you_want_to_be_integer))
alert( "Converted result: " + (parseInt(num1) + num2));

// Converts to Strings to Decimal.
let pi = parseFloat("3.14");

/* Pag nagconvert ka ng float into integer ay magiging whole number siya at laging round down
*/ 
